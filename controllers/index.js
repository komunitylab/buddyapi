'use strict';

const account = require('./account'),
      auth = require('./auth'),
      authorize = require('./authorize'),
      userLanguages = require('./user-languages'),
      users = require('./users');

module.exports = { account, auth, authorize, userLanguages, users };

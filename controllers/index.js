'use strict';

const account = require('./account'),
      auth = require('./auth'),
      authorize = require('./authorize'),
      users = require('./users');

module.exports = { account, auth, authorize, users };

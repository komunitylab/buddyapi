'use strict';

module.exports = {
  database: {
    password: 'pwd' // set a good, strong password
  },
  jwt: {
    secret: 'secret' // set a very long, very strong secret. can be changed once in a while for better security
  }
};

'use strict';

const definitions = require('./definitions'),
      account = require('./account'),
      auth = require('./auth'),
      userLanguages = require('./user-languages'),
      users = require('./users');

module.exports = { definitions, account, auth, userLanguages, users };

'use strict';

const definitions = require('./definitions'),
      account = require('./account'),
      auth = require('./auth'),
      messages = require('./messages'),
      userLanguages = require('./user-languages'),
      users = require('./users');

module.exports = { definitions, account, auth, messages, userLanguages, users };
